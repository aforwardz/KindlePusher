<!--pages/account/account.wxml-->
<view class="container">
    <!-- 用户登录测试 -->
    <view class="userinfo" bindtap="login">
        <image
            class="userinfo-avatar"
            src="{{logged ? userInfo.avatarUrl : '../../assets/images/user-unlogin.png'}}"
            background-size="cover"
        ></image>
        <view>
            <text class="userinfo-nickname">{{logged ? userInfo.nickName : '点击登录'}}</text>
            <text class="userinfo-nickname" wx:if="{{logged}}">{{userInfo.lastLoginTime}}</text>
        </view>
    </view>
    <view class='userdata' bindtap='login'>
<<<<<<< HEAD
      <view wx:for="{{userdata}}">
        {{item.name}}:{{item.value}}
      </view>
=======
      <text class='data-item' wx:for="{{userData}}" wx:key="k">{{item.v}}</text>
>>>>>>> f36f47bedfe8f683f302e59019a0745a50291e74
    </view>
</view>